import React from 'react';

import './header.scss';

const Header = () => (
  <header className="header">
    <h1 className="main-title">WeatherApp</h1>
    {/* <div className="theme-toggle">AA</div> */}
  </header>
);

// Header.propTypes = {
//   search: PropTypes.string,
//   setSearch: PropTypes.func,
// };

export default Header;
